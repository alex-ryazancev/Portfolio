include ./mixins.pug

<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title Portfolio | main page
  body
    .wrapper
      .welcome-section
        section.hero
          .container.hero__container
            header.header.hero__header
              .hero__logo
                .logo BA
              .hero__links 
                nav.nav
                  - var menu = ['Главная', "Обо мне", "Навыки", "Работы", "Отзывы", "Связатся"]
                  ul.nav__list
                    each item in menu
                      li.nav__item
                        a(href="").nav__link #{item}  
              .hero__menu
                - var icons = ['insta', 'vk', 'github']
                ul.socials
                  each item, index in icons
                    li.socials__item(class=`${index === 1 ? 'active' : ''}`)
                      a(href="").socials__link
                        +icon(`${item}`, 'socials__icon')
            .hero__content
              .user-info
                p.user-info__desc Личный сайт веб разработчика
                p.user-info__username Владимир Астаханов

            .hero__btns
              .scroll-down__btns Скролл
              - var icons = ['scroll']
                each item in icons
                  .scroll-down__item
                    a(href="").scroll-down__link
                      +icon(`${item}`, 'scroll-down__icon')       
          
        section.about
          .container.about__container
            .about__left
              .user
                .avatar.user__avatar
                  +image("content/user.jpg", "avatar__pic")
                ul.user__desc
                  - 
                    var items = [
                      ['Проживаю:', 'Новосибирск'],
                      ['Возраст:', '27 лет'],
                      ['Родился:', '1 января 1991'],
                    ]
                  each item in items
                    li.user__desc-item
                      .user__key #{item[0]}
                      .user__value #{item[1]}
            .about__right
              h4.about__prolog Привет, меня зовут
              h2.about__title Владимир Астаханов
              .about__text Я занимаюсь разработкой современных сайтов и приложений. Мне нравится делать интересные современные проекты.
              .about__ps Этот сайт я сделал в рамках обучения в Школе онлайн образования LoftSchool. Чуть позже я освежу в нем свой контент. А пока посмотрите как тут все классно и красиво!

      .triangles
        svg.triangles__item(viewBox="0 0 1000 100" preserveAspectRatio="none")
          polygon(points="0,0 0,100 1000,100")
        svg.triangles__item(viewBox="0 0 1000 100" preserveAspectRatio="none")
          polygon(points="0,100 1000,100 1000,0")

      .values
        .container
          .values__title
            h2.section-title.values__title-text Чем я могу быть вам полезен
            .values__desc
              p Этот сайт я сделал в рамках обучения в Школе онлайн образования LoftSchool. Чуть позже я освежу в нем свой контент. А пока посмотрите как тут все классно и красиво!
          //- .input
          //-   input(type="text").input__elem
          //-   .input__icon
          .values__content
            .values__img
              +image("content/bag.png", "values__pic")
            ul.user-skills
              - 
                var skills = [
                  ['Front-end', ['HTML5', 'CSS3', 'Javascript', 'Jquery и Vue.js']],
                  ['Workflow', ['GIT', 'Terminal', 'Gulp', 'Webpack']],
                ]
              each item in skills 
                li.user-skills__item.skills-row 
                  h3.skills-row__title #{item[0]}
                  ul.skills-row__list
                    each skill in item[1]
                      li.skills-row__item
                        .skill
                          svg(viewBox="0 0 200 200" preserveAspectRatio="none").skill__shape
                            circle(r="80" cx="100" cy="100").skill__circle.skill__circle--position--under
                            //- circle(r="78" cx="100" cy="100").skill__circle.skill__circle--position--above
                            linear-gradient()
                          .skill__title #{skill}
                      
      .works
        h2.section-title Мои работы
        .works__left
          .slider
            img().slider__fillpic
            .slider__preview
              ul.previews
                - for (var i = 0; i < 3; i++)
                  li.previews__item
                    img.previews__pic
            .slider__buttons
              .slider-button
                button.slider-buttons__item.slider-buttons__item--slide--prev 
                button.slider-buttons__item.slider-buttons__item--slide--next

        .works__right
          .works__tags
            - var tags = ['html', 'css', 'javascript']
            each tag in tags
              ul.tags
                li.tags__item #{tag}
          .works__text
            h3.works__title Сайт школы
            .works__desc-text
              p Этот парень проходил обучение веб-разработке не где-то, а в Loftschool! 2 месяца только самых тяжелых испытаний и бессонных ночей<!DOCTYPE html>
              html(lang="en")
                head
                  meta(charset="UTF-8")
                  meta(name="viewport", content="width=device-width, initial-scale=1.0")
                  title Document
                body
            a.iconed-link
              .iconed-link__icon
              .iconed-link__text
      .triangles.works-triangles
        svg.triangles__item(viewBox="0 0 1000 100" preserveAspectRatio="none")
          polygon(points="0,0 0,100 1000,100")
        svg.triangles__item(viewBox="0 0 1000 100" preserveAspectRatio="none")
          polygon(points="0,100 1000,100 1000,0")

      .reviews
        .reviews__img
          +image("content/budda.jpg", "reviews__pic")

      footer.footer
        .container.footer__container
          .footer__top
            .footer__logo
              .logo &copy; 2019
            .footer__links 
              nav.nav
                - var menu = ['Главная', "Обо мне", "Навыки", "Работы", "Отзывы", "Связатся"]
                ul.nav__list
                  each item in menu
                    li.nav__item
                      a(href="").nav__link.footer__link #{item}  
          .footer__bottom
            .footer__menu
              - var icons = ['insta', 'vk', 'github']
              ul.socials
                each item, index in icons
                  li.socials__item(class=`${index === 1 ? 'active' : ''}`)
                    a(href="").socials__link
                      +icon(`${item}`, 'socials__icon footer__icon')

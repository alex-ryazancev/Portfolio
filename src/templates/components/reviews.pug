script(type="text/x-template")#reviews-section
  section.reviews(@click="checkArrows();" @touchend="checkArrows();")
    .reviews__header
      .reviews__title.section-title Что обо мне говорят 
      .reviews__buttons
        button.reviews__btn.reviews__btn_prev(@click="previous" :disabled="true")
        button.reviews__btn.reviews__btn_next(@click="next" :disabled="false")
    flickity(ref="flickity" :options="flickityOptions").reviews__content
      .reviews__content-wrapper
        - for (var i = 0; i<5; i++)
          .reviews__content-item
            .quote
              .quote__text 
                .text-quote__text Этот парень проходил обучение веб-разработке не где-то, а в Loftschool! 4,5 месяца только самых тяжелых испытаний и бессонных ночей
                .text-quote__author 
              .user-min 
                .user-min__avatar
                  .user-min__pic-wrapper 
                    +image('content/user.webp', 'user__avatar-pic')
                .user__data
                  .user-min__name Ковальчук Дмитрий
                  .user-min__occ Основатель Loftschool